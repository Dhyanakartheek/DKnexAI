# DKnexAI Unified Production Requirements
# One shared venv for all 4 Python agents
# Install with: pip install -r requirements-production.txt

# ── Core FastAPI stack ────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.1
httpx==0.27.2
starlette==0.41.3

# ── Groq AI (Agent_chat, Agent_coding, Agent_email, Agent_rag) ───────────────
groq==0.12.0

# ── LangChain + FAISS (Agent_rag) ────────────────────────────────────────────
langchain==0.3.7
langchain-community==0.3.7
langchain-core==0.3.15
langchain-groq==0.2.1
langgraph==0.2.48
faiss-cpu==1.9.0
sentence-transformers==3.3.1

# ── PyTorch CPU (for sentence-transformers — no GPU needed on EC2 t-series) ──
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
torchvision==0.20.1+cpu

# ── Agent_email database & auth ───────────────────────────────────────────────
sqlalchemy==2.0.36
aiosqlite==0.20.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.12

# ── Agent_email scheduler & utilities ────────────────────────────────────────
apscheduler==3.10.4
openpyxl==3.1.5
reportlab==4.2.5
jinja2==3.1.4
aiofiles==24.1.0
email-validator==2.2.0
